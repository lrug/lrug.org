---
status: 'Published'
---

<%
  meetings = meeting_pages.select { |page| page.path.include? "/#{locals[:year]}/" }
  content_for(:title) { "#{locals[:year]} Meetings" }
  content_for(:rss_path) { 'meetings/' }
%>
<%= partial 'meetings/meeting', collection: meetings %>
